{
  "name": "frag_missing_close_tag",
  "classification": "correct",
  "description": "Unclosed <Say> (no </Say>) is now rejected LOUDLY by the real parser (mismatched closing tag), so the engine takes the fallback path instead of silently dropping text. Malformed XML must never execute partial behavior.",
  "ideal": "",
  "base_url": "https://voice.example.com/ivr/voice",
  "input_xml": "<Response><Say>Hello<Hangup/></Response>",
  "expected_parse": {
    "ok": false,
    "error": "malformed XML: mismatched closing tag: expected </Say> but found </Response>"
  },
  "expected_trace": []
}
