# CI-ONLY dummy environment for `docker compose config` validation.
# =============================================================================
# These are OBVIOUSLY FAKE placeholder values (TEST-NET IPs / "ci-dummy-*")
# that exist so compose interpolation — including the intentional fail-closed
# `${VAR:?}` guards (TURN_SECRET, GRAFANA_ADMIN_PASSWORD, replica vars) — can
# be exercised in CI without real secrets. Never source this on a VM.
# =============================================================================
EXTERNAL_SIP_IP=198.51.100.10
EXTERNAL_RTP_IP=198.51.100.10
FREESWITCH_IP=198.51.100.20
FS_PUBLIC_IP=198.51.100.20
SBC_INTERNAL_IP=10.0.0.2
SBC_PROXY_IP=10.0.0.2
SBC_PROXY_IP_FAILOVER=10.0.0.3
HOMER_IP=10.0.0.4
API_HOST=10.0.0.4
DB_HOST=10.0.0.4
DB_PASS=ci-dummy-db-pass
DATABASE_URL=postgresql://api:ci-dummy@host.docker.internal:6432/voip
REDIS_URL=redis://10.0.0.5:6379
ESL_PASSWORD=ci-dummy-esl
FREESWITCH_ESL_HOST=10.0.0.5
FREESWITCH_ESL_PASSWORD=ci-dummy-esl
JWT_SECRET_KEY=ci-dummy-jwt-secret
INGEST_SHARED_SECRET=ci-dummy-ingest-secret
CORS_ORIGINS=https://ci.invalid
TURN_SECRET=ci-dummy-turn-secret
TURN_HOST=198.51.100.20
VERTO_WS_URL=wss://ci.invalid:8082
STORAGE_ACCESS_KEY=ci-dummy-access
STORAGE_SECRET_KEY=ci-dummy-secret
GRAFANA_ADMIN_PASSWORD=ci-dummy-grafana
REPLICA_PRIMARY_HOST=10.0.0.4
REPL_PASSWORD=ci-dummy-repl
