// Re-export the useToast hook and ToastProvider from ToastContext for convenience.
// Import from here when you only need the hook:
//   import { useToast } from '@/components/ui/Toast'
export { useToast, ToastProvider } from './ToastContext';
