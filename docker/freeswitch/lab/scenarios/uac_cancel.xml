<?xml version="1.0" encoding="UTF-8" ?>
<!--
  Call-quality lab check #9: the callee never answers; the caller CANCELs
  after 5 s of ringing. Pair with uas_noanswer.xml. No RTP is sent.
  Expect: A and B CDRs unanswered -> quality_status 'unanswered', mos NULL
  (FS legacy rtp_audio_in_mos may still read 4.50 -> only in fs_mos).
-->
<scenario name="RCF quality lab UAC (ring then CANCEL)">

  <send retrans="500">
    <![CDATA[
      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "lab" <sip:lab@[local_ip]:[local_port]>;tag=[pid]LabUac[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:lab@[local_ip]:[local_port]>
      Max-Forwards: 70
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=lab 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0 101
      a=rtpmap:0 PCMU/8000
      a=rtpmap:101 telephone-event/8000
      a=fmtp:101 0-16
      a=ptime:20
      a=sendrecv

    ]]>
  </send>

  <recv response="100" optional="true"/>
  <recv response="180"/>

  <pause milliseconds="5000"/>

  <!-- CANCEL: same Request-URI, Call-ID, From, To (no tag), CSeq number and
       Via branch as the INVITE ([last_Via:] of the 180 = our INVITE Via). -->
  <send>
    <![CDATA[
      CANCEL sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      [last_Via:]
      From: "lab" <sip:lab@[local_ip]:[local_port]>;tag=[pid]LabUac[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 CANCEL
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>

  <recv response="200"/>
  <recv response="487"/>

  <send>
    <![CDATA[
      ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      [last_Via:]
      From: "lab" <sip:lab@[local_ip]:[local_port]>;tag=[pid]LabUac[call_number]
      [last_To:]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>
</scenario>
