# High-performance async stack
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
uvloop>=0.19.0
httptools>=0.6.0

# Database - async PostgreSQL with connection pooling
asyncpg>=0.29.0

# Redis - async client
redis>=5.0.0

# Validation and serialization
pydantic>=2.5.0
# UCaaS config models (settings) — restored with the UCaaS routers
pydantic-settings>=2.1.0

# Fast JSON (10x faster than standard json)
orjson>=3.9.0

# HTTP client for webhooks
httpx>=0.26.0

# Object storage (S3-compatible: MinIO locally, S3/GCS in prod) — Phase 4
# voicemail / conference recordings / file uploads
boto3>=1.34.0
# UCaaS async HTTP + FreeSWITCH ESL client (webrtc/voicemail; switchio replaces
# greenswitch in Phase 5 — kept for now so the UCaaS routers import cleanly)
aiohttp>=3.9.0
greenswitch>=0.0.5

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0

# Utilities
python-multipart>=0.0.6
