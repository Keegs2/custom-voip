# High-performance async stack
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
uvloop>=0.19.0
httptools>=0.6.0

# Database - async PostgreSQL with connection pooling
asyncpg>=0.29.0

# Redis - async client
redis>=5.0.0

# Validation and serialization
pydantic>=2.5.0
# UCaaS config models (settings) — restored with the UCaaS routers
pydantic-settings>=2.1.0

# Fast JSON (10x faster than standard json)
orjson>=3.9.0

# HTTP client for webhooks
httpx>=0.26.0

# Object storage (S3-compatible: MinIO locally, S3/GCS in prod) — Phase 4
# voicemail / conference recordings / file uploads
boto3>=1.34.0
# UCaaS async HTTP (webrtc/voicemail helpers)
aiohttp>=3.9.0
# Phase 5 — ESL control plane:
#   * switchio was REJECTED — its only PyPI releases (0.1.0a0/0.1.0a1) use
#     @asyncio.coroutine (removed in Python 3.11) + an undeclared `six` dep and
#     cannot import on Python 3.12. See PRODUCTION_READINESS_PLAN.md Decision Log.
#   * greenswitch was REMOVED — gevent-based, conflicts with asyncio/uvloop.
# We use our OWN persistent asyncio ESL client (services/esl_client.py): zero new
# dependencies, native asyncio, full event + command multiplexing.

# Token encryption at rest (Fernet) for the Calendar integration AND envelope
# encryption (AES-256-GCM per-object DEK + MultiFernet KEK wrap) for the
# encrypted-at-rest Visual Voicemail product (services/voicemail_crypto.py).
# Transitively present via python-jose[cryptography]; pinned explicitly so the
# dependency is not silently dropped if jose's extras change.
cryptography>=42.0.0
# Optional — production envelope-KEK provider for Visual Voicemail
# (GcpKmsProvider, Cloud HSM / FIPS 140-2 L3). Not required for the local
# MinIO/LocalKmsProvider stack; install only on the prod Services VM.
# google-cloud-kms>=2.21.0

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0

# Utilities
python-multipart>=0.0.6
