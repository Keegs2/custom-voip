<?xml version="1.0" encoding="UTF-8" ?>
<!--
  uac_short_call.xml - SIPp UAC scenario with SDP and configurable hold time.

  Purpose: Make real calls through the full SBC/FreeSWITCH stack.  The built-in
  "uac" scenario does not include an SDP body, so FreeSWITCH cannot process the
  call properly.  This scenario sends an INVITE with PCMU SDP, waits for the
  answer, holds for -d milliseconds (SIPp's default pause duration), then sends
  BYE.

  Usage:
    sipp TARGET -sf /app/scenarios/uac_short_call.xml -s 9196 -d 2000 ...

  The -d flag sets the <pause/> duration in milliseconds (default 1000 if
  omitted).
-->
<scenario name="UAC short call with SDP">

  <!-- INVITE with SDP (PCMU/8000) -->
  <send retrans="500">
    <![CDATA[
      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "SIPp" <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:sipp@[local_ip]:[local_port]>
      Max-Forwards: 70
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=sipp 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=SIPp Call
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0
      a=rtpmap:0 PCMU/8000
    ]]>
  </send>

  <!-- Absorb optional 100 Trying -->
  <recv response="100" optional="true" />

  <!-- Absorb optional 180 Ringing / 183 Session Progress -->
  <recv response="180" optional="true" />
  <recv response="183" optional="true" />

  <!-- Wait for 200 OK (call answered) -->
  <recv response="200" rtd="true" />

  <!-- ACK the 200 -->
  <send>
    <![CDATA[
      ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "SIPp" <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: <sip:sipp@[local_ip]:[local_port]>
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <!-- Hold the call for -d milliseconds (default 1000ms) -->
  <pause />

  <!-- BYE to tear down the call -->
  <send retrans="500">
    <![CDATA[
      BYE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "SIPp" <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Contact: <sip:sipp@[local_ip]:[local_port]>
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <!-- Wait for 200 OK to BYE -->
  <recv response="200" />

  <!-- Response time repartition table -->
  <ResponseTimeRepartition value="10, 20, 30, 40, 50, 100, 150, 200, 500, 1000" />

</scenario>
