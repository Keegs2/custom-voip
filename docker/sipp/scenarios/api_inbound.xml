<?xml version="1.0" encoding="UTF-8" ?>
<!--
  api_inbound.xml — SIPp UAC modelling an INBOUND call to an API-voice DID.

  Product: API Calling (programmable voice / webhook-driven).

  Ladder modelled (scripts/CLAUDE.md, product_type == "api"):
    SIPp (acts as Bandwidth)  ->  SBC VIP :5060  ->  FreeSWITCH :5080
      INVITE  sip:<API_DID>@<VIP>
      100 Trying
      200 OK            (inbound_router.lua ANSWERS immediately, then hands off
                         to voice_webhook.lua — no carrier B-leg, so typically
                         NO 180/183; the 200 OK comes from FreeSWITCH itself)
      ACK
      ... voice_webhook plays Say/Play/Gather verbs (early/answered media) ...
      BYE / 200 OK

  Because the API product answers locally and runs the TwiML-style engine, the
  call is answered by FreeSWITCH (not a forwarded PSTN party). The DID must
  exist in api_dids with a reachable voice_url that returns a <Response> doc.

  Run:
    sipp <VIP>:5060 -sf /app/scenarios/api_inbound.xml \
         -s 18005551234 -d 4000 -m 1 -trace_err
  (-s = API DID, -d = how long to stay up while the webhook plays media.)
-->
<scenario name="API-voice inbound to SBC -> FreeSWITCH (voice_webhook)">

  <send retrans="500">
    <![CDATA[
      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "API Caller" <sip:15551112222@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:15551112222@[local_ip]:[local_port]>
      Max-Forwards: 70
      Supported: timer
      User-Agent: SIPp-API-Inbound
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=sipp 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=SIPp Call
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0
      a=rtpmap:0 PCMU/8000
    ]]>
  </send>

  <recv response="100" optional="true" />
  <!-- API product answers locally; provisional responses are uncommon but allowed. -->
  <recv response="180" optional="true" />
  <recv response="183" optional="true" />

  <recv response="200" rtd="true" />

  <send>
    <![CDATA[
      ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "API Caller" <sip:15551112222@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: <sip:15551112222@[local_ip]:[local_port]>
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <!-- Stay up while voice_webhook.lua executes verbs (-d ms). -->
  <pause />

  <send retrans="500">
    <![CDATA[
      BYE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "API Caller" <sip:15551112222@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Contact: <sip:15551112222@[local_ip]:[local_port]>
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <recv response="200" />

  <ResponseTimeRepartition value="10, 50, 100, 200, 500, 1000, 2000" />

</scenario>
