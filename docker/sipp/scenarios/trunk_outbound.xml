<?xml version="1.0" encoding="UTF-8" ?>
<!--
  trunk_outbound.xml — SIPp UAC modelling a customer PBX placing an OUTBOUND
  call through a SIP trunk to the PSTN.

  Product: SIP Trunking (IP-authenticated).

  Ladder modelled (kamailio/CLAUDE.md 7.3 + scripts/CLAUDE.md trunk_outbound.lua):
    SIPp (acts as customer PBX)  ->  SBC :5060  ->  FreeSWITCH :5080
      INVITE  sip:<PSTN_DEST>@<SBC>
        - Kamailio route[TRUNK_AUTH] authenticates the SIPp source IP against
          trunk_auth_ips, appends X-Trunk-ID / X-Customer-ID, dispatches to FS.
        - FS runs trunk_outbound.lua: validates the From DID belongs to the
          trunk, sets caller ID, bridges out via sofia/external -> Kamailio ->
          Bandwidth (X-Carrier=primary, failover secondary).
      100 Trying
      183 / 180         (carrier early media / ringback)
      200 OK
      ACK
      ... media ...
      BYE / 200 OK

  CRITICAL preconditions (otherwise Kamailio returns 403 / FS rejects):
    1. The SIPp source IP MUST be in trunk_auth_ips for an enabled trunk
       (IP authentication — there is no digest auth). For local testing,
       set TESTING_IP in the SBC .env to the SIPp host, OR run SIPp from a
       host whose IP is provisioned.
    2. The From user (caller DID, 15559998888 below) MUST be a DID assigned to
       that trunk in trunk_dids, or trunk_outbound.lua falls back to the trunk's
       default DID; if none, it rejects with CALL_REJECTED.

  Run:
    sipp <SBC_IP>:5060 -sf /app/scenarios/trunk_outbound.xml \
         -s 17742184477 -d 3000 -m 1 -trace_err
  (-s = PSTN destination, From user = the trunk's outbound caller DID.)
-->
<scenario name="Trunk outbound: PBX -> SBC -> FreeSWITCH -> carrier">

  <send retrans="500">
    <![CDATA[
      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "Trunk DID" <sip:15559998888@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:15559998888@[local_ip]:[local_port]>
      Max-Forwards: 70
      Supported: timer
      Session-Expires: 1800;refresher=uac
      Min-SE: 90
      User-Agent: SIPp-Trunk-PBX
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=sipp 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=SIPp Call
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0
      a=rtpmap:0 PCMU/8000
    ]]>
  </send>

  <recv response="100" optional="true" />
  <recv response="180" optional="true" />
  <recv response="183" optional="true" />

  <recv response="200" rtd="true" />

  <send>
    <![CDATA[
      ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "Trunk DID" <sip:15559998888@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: <sip:15559998888@[local_ip]:[local_port]>
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <pause />

  <send retrans="500">
    <![CDATA[
      BYE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "Trunk DID" <sip:15559998888@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Contact: <sip:15559998888@[local_ip]:[local_port]>
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <recv response="200" />

  <ResponseTimeRepartition value="10, 50, 100, 200, 500, 1000, 2000" />
  <CallLengthRepartition value="10, 100, 500, 1000, 5000" />

</scenario>
